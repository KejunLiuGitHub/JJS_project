# %% [markdown]
r"""
## Raw Data Ingestion

The raw data are Bruker NanoScope Analysis `.txt` files.
Each file contains 512 data points with 2 Chinese header lines.

In the raw export, $Z$ decreases monotonically.
"""
